name: Build APK
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      
      # Сначала генерируем чистую структуру, чтобы появились все нужные Gradle файлы
      - run: flutter create --platforms=android .
      
      # Переносим наш заранее заготовленный правильный манифест на его законное место
      - run: cp custom_manifest.xml android/app/src/main/AndroidManifest.xml
      
      - run: flutter pub get
      - run: flutter build apk --release
      
      - uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
